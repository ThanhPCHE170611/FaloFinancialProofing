IOT

add thư viện ở libary Manager

sử bộ điều khiển:
- tải code bộ điều khiển trên web(github)
- add thư viện: (#include <IRremote.h> 
- decode_results tên thiết bị ( đặt tên thiết bị)
- result tên thiết bị
- seriol moniter () : sử dụng để truyền nhận dữ liệu thông tin
- (true) check đã nhận dữ liệu (vd: tên.blink13(true))
- .enebleIRIn() :   
- .decode() : 
- Serial.println(tên.value, HEX) : in ra mã nút ở serial monitor để đặt mã vào giá trị (HEX là dạng hexadecimal)
- val = analogRead(analogPin); : đọc dữ liệu pin nhập
- 
switch(tên.value) {
case: mã phím trên remote;
lệnh cài đặt cho phím;
}

- tên.resume


LM35 -  cảm biến nhiệt độ
( tín hiệu nhận về Analog)
- chú ý khi cắm: 	chân 3 - GND:  
			chân 2 - output: 1 độ - 100v
			chân 1 -  VCC: 4 - 20v DC
- đọc dữ liệu (AnalogRead)

MÀN ICD

- 
